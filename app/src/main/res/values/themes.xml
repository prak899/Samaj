<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SampleTextInputLayoutStyle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColorHint">@color/colorDarkGrey</item>
        <item name="android:textColorPrimary">@color/colorDarkGrey</item>
    </style>

    <style name="SampleEditTextStyle" parent="android:Widget.EditText">
        <item name="android:textSize">18sp</item>
        <item name="android:textColorPrimary">@color/colorDarkGrey</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingStart">8dp</item>
    </style>

    <style name="SampleCheckboxStyle" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="buttonTint">@color/colorDarkGrey</item>
        <item name="android:textSize">18sp</item>
    </style>


    <style name="errorDialogButton" parent="android:Widget.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:background">@drawable/error_button_background</item>
        <item name="android:textSize">@dimen/default_error_text_size</item>
        <item name="android:paddingLeft">@dimen/error_button_padding</item>
        <item name="android:paddingRight">@dimen/error_button_padding</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="MatchParent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>
</resources>
